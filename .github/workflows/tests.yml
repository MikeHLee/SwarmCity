name: Tests

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  test:
    name: ${{ matrix.os }} / Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install .[ai] pytest anyio

      - name: List installed packages
        run: pip list

      - name: Run tests
        env:
          PYTHONPATH: src
        run: |
          pytest --tb=short

      - name: Debug on failure
        if: failure()
        run: |
          echo "Listing .swarm directory if it exists"
          ls -R .swarm || echo ".swarm not found"
          echo "Last 50 lines of pytest output would be useful here, but pytest is run in the previous step."
